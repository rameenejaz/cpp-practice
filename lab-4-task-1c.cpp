#include <iostream> 
using namespace std;
int main () {
    int x=10;
    int y=15;
    int z=20;

    if (x != 5 ) && (y != z) || (x + y >= z) {
        cout<<"True";
    }
    else {
        cout<<"False";
    }
}
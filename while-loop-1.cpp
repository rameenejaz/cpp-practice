#include <iostream>
using namespace std;
int main () {
    int number=0;
    int sum=0;
    int i=0;

    cout<<"Enter a positive number: ";
    cin>>number;

    while (number>=i) {
        sum=sum+i;
        i++;

    }
    cout<<"The sum of the numbers is: "<<sum;
}